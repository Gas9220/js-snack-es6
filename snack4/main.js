'use strict';

// Functions ---------------------------------

// Funzione che facilita la creazione di una squadra
function createTeam(teamName) {
    const team = {
        teamName: teamName,
        points: 0,
        fouls: 0
    }

    return team;
}

// -------------------------------------------
