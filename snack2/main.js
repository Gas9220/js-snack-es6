'use strict';

//------------- Funtions -------------------

// Funzione che facilita la creazione di un singolo studente
function createStudent(id, name, grade) {
    let student = {
        id: id,
        name: name,
        grade: grade
    }

    return student;
}

//-----------------------------------------
